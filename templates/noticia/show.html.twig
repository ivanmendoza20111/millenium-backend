{% extends 'base.html.twig' %}

{% block title %}Noticia{% endblock %}

{% block body %}
    <h1>Noticia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ noticium.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ noticium.titulo }}</td>
            </tr>
            <tr>
                <th>Medio</th>
                <td>{{ noticium.medio }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ noticium.fecha ? noticium.fecha|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Archivo</th>
                <td>{{ noticium.archivo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_noticia_index') }}">back to list</a>

    <a href="{{ path('app_noticia_edit', {'id': noticium.id}) }}">edit</a>

    {{ include('noticia/_delete_form.html.twig') }}
{% endblock %}
